/*

USE [E-commerce]
GO

SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE TRIGGER [dbo].[SetDefaultRole] ON [dbo].[Contas]
AFTER INSERT 
AS
BEGIN
	UPDATE Contas
	SET Role = 2
	WHERE Role is NULL;
	PRINT 'Default values updated';

END;


-- Este script serve para deixar o usuario como cliente quando ele é criado--

*/

